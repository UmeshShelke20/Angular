import { MouseEffectDirective } from './mouse-effect.directive';

describe('MouseEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseEffectDirective();
    expect(directive).toBeTruthy();
  });
});
