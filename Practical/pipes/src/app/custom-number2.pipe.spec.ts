import { CustomNumber2Pipe } from './custom-number2.pipe';

describe('CustomNumber2Pipe', () => {
  it('create an instance', () => {
    const pipe = new CustomNumber2Pipe();
    expect(pipe).toBeTruthy();
  });
});
