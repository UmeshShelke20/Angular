import { LengthOfNumberPipe } from './length-of-number.pipe';

describe('LengthOfNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new LengthOfNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
